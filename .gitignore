# Local .terraform directories
**/.terraform/*

# .tfstate files (Chứa thông tin nhạy cảm về hạ tầng hiện tại)
*.tfstate
*.tfstate.*

# Crash log files
crash.log

# Exclude all .tfvars files, which are likely to contain sensitive data
*.tfvars

# Ignore Private Key files (KHÔNG BAO GIỜ UP KEY LÊN GITHUB)
*.pem

# Ignore override files as they are usually local customizations
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Editor directories
.idea/
.vscode/
